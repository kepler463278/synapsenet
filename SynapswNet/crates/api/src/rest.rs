// REST API server (placeholder)

pub struct RestServer;

impl RestServer {
    pub fn new() -> Self {
        Self
    }
}

impl Default for RestServer {
    fn default() -> Self {
        Self::new()
    }
}
