// JSON-RPC server (placeholder)

pub struct RpcServer;

impl RpcServer {
    pub fn new() -> Self {
        Self
    }
}

impl Default for RpcServer {
    fn default() -> Self {
        Self::new()
    }
}
